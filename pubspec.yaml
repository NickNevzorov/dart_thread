name: dart_thread
description: Classic thread implementation (such as TThread in Delphi). Single code for all platforms (mapping to isolate on desktop or to worker on web).
version: 0.0.6
homepage: https://github.com/NickNevzorov/dart_thread

environment:
  sdk: ">=2.15.0 <4.0.0"
